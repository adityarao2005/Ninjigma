<UserControl
    x:Class="Ninjigma.UniformGrid"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Ninjigma"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

	<UserControl.Resources>

		<Style TargetType="Grid">
			<Style.Setters>
				<Setter Property="Background" Value="{x:Bind Background}" />
				<Setter Property="BorderBrush" Value="{x:Bind BorderBrush}" />
			</Style.Setters>
			
		</Style>
	</UserControl.Resources>

	<Grid x:Name="grid" BorderThickness="{x:Bind BorderThickness}" />

</UserControl>
